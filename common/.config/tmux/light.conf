set -g @bg_dim      '#efebd4'
set -g @bg0         '#fdf6e3'
set -g @bg1         '#f4f0d9'
set -g @bg2         '#efebd4'
set -g @bg3         '#e6e2cc'
set -g @bg4         '#e0dcc7'
set -g @bg_visual   '#eaedc8'
set -g @fg          '#5c6a72'
set -g @red         '#f85552'
set -g @yellow      '#dfa000'
set -g @green       '#8da101'
set -g @blue        '#3a94c5'
set -g @purple      '#df69ba'
set -g @aqua        '#35a77c'
set -g @orange      '#f57d26'
set -g @grey0       '#a6b0a0'
set -g @grey1       '#939f91'
set -g @statusline1 '#93b259'
set -g @statusline2 '#e66868'

# ---- General Settings ----
set-option -g status "on"
set -g status-interval 2

set-option -g status-fg '#5c6a72' # fg
set-option -g status-bg '#fdf6e3' # bg0

set-option -g mode-style fg='#{@purple}',bg='#{@bg2}'

# default statusbar colors
set-option -g status-style fg='#{@fg}',bg='#{@bg0}',default

# ---- Windows ----
# default window title colors
set-window-option -g window-status-style fg='#{@bg4}',bg='#{@bg0}'

# default window with an activity alert
set-window-option -g window-status-activity-style 'bg=#{@bg1},fg=#{@bg3}'

# active window title colors
set-window-option -g window-status-current-style fg='#{@fg}',bg='#{@bg_visual}'

# ---- Pane ----
# pane borders
set-option -g pane-border-style fg='#{@bg1}'
set-option -g pane-active-border-style 'fg=#{@blue}'

# pane number display
set-option -g display-panes-active-colour '#3a94c5' # blue
set-option -g display-panes-colour '#f57d26' # orange

# ---- Command ----
# message info
set-option -g message-style fg='#{@statusline2}',bg='#{@bg_dim}'

# writing commands inactive
set-option -g message-command-style 'fg=#{@bg3},bg=#{@bg1}'

# ---- Miscellaneous ----
# clock
set-window-option -g clock-mode-colour '#3a94c5' # blue

# bell
set-window-option -g window-status-bell-style fg='#{@bg0}',bg='#{@statusline3}'

# ---- Formatting ----
set-option -g status-left-style none
set -g status-left-length 60
# bg here is statusline1, fg is the dark bg0 for contrast
set -g status-left "#[fg=#fdf6e3,bg=#93b259,bold] #S "

set-option -g status-right-style none
set -g status-right-length 150
set -g status-right ''

set -g window-status-separator ''
set -g window-status-format "#[fg=#{@grey0},bg=#{@bg0}] #I | #[fg=#{@grey0},bg=#{@bg0}]#W "
set -g window-status-current-format "#[fg=#{@grey1},bg=#{@bg3}] #I | #[fg=#{@grey1},bg=#{@bg3},bold,italics]#W "
