-- Disnostic
vim.diagnostic.config({
  -- Text shows up underneath the line, with virtual lines, instead of at the end of line
  virtual_text = false,
  virtual_lines = true,
})

-- Disable animations
vim.g.snacks_animate = false

-- Disable autoformat
vim.g.autoformat = false
